mixin home_paginator()
    - var prev = page.prev_link
    - var next = page.next_link
    .paginator
        if page.prev
            a.prev(href=url_for(prev)) 
                    i.iconfont.icon-angle-left
                    span 
                        != __('prev')
        if page.next
            a.next(href=url_for(next)) 
                    span
                        != __('next')
                    i.iconfont.icon-angle-right

mixin post_paginator()
    - var prev = page.prev ? page.prev.path : false;
    - var next = page.next ? page.next.path : false;
    .paginator
        if prev
            a.prev(href=url_for(prev))
                i.iconfont.icon-angle-left
                span 
                    != __('prev')
        if next
            a.next(href=url_for(next))
                span
                    != __('next')
                i.iconfont.icon-angle-right
